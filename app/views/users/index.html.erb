<div class="header">User</div>

<table>
  <thead>
    <th>image</th>
    <th>name</th>
    <th></th>
  </thead>
  <% @users.each do |user| %>
  <tbody>
    <th>image</th>
    <th><%= user.name %></th>
    <th><%= link_to "Show", user_path(user.id) %></th>
  </tbody>
  <% end %>
</table>