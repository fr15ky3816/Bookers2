<div class="header">Books</div>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td>image</td>
      <td> <%= book.title %> </td>
      <td> <%= book.opinion %> </td>
      <% end %>
    </tr>
  </tbody>
</table>

<%= render partial: 'layouts/left_partial', locals: { user: @user, book: @book } %>