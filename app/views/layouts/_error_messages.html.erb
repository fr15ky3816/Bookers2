<% if book.errors.any? %>
  <div class="alert alert-warning">
    <h3 class="error-message"><%= book.errors.full_messages.count %> error prohibited this obj from being saved:</h3>
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
